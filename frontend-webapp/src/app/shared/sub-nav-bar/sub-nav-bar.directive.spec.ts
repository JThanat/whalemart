import { SubNavBarDirective } from './sub-nav-bar.directive';

describe('SubNavBarDirective', () => {
  it('should create an instance', () => {
    // TODO: Write test
    const directive = new SubNavBarDirective(null as any, null as any, null as any);
    expect(directive).toBeTruthy();
  });
});
